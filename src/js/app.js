import * as functions from './modules/baseFunctions.js';

functions.isWebp();

new VenoBox({
  selector: '.my-image-links',
  numeration: true,
  infinigall: true,
  share: true,
  spinner: 'rotating-plane'
});
